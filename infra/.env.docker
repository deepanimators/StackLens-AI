# Docker Infrastructure Environment Variables
# Works on Windows EC2, Linux, and macOS

# Kafka Configuration
# For Windows EC2: Set this to your EC2 public IP or domain
# For local development: Use localhost
KAFKA_EXTERNAL_HOST=localhost

# PostgreSQL Configuration
POSTGRES_USER=stacklens
POSTGRES_PASSWORD=password
POSTGRES_DB=stacklens

# Elasticsearch Configuration
ES_JAVA_OPTS=-Xms512m -Xmx512m

# For Windows EC2 Deployment:
# 1. Update KAFKA_EXTERNAL_HOST to your EC2 public IP
# 2. Ensure Windows Firewall allows ports: 2181, 9094, 4317, 4318, 9200, 5432, 16686
# 3. Install Docker Desktop for Windows (WSL2 backend recommended)
# 4. Run: docker-compose up -d
